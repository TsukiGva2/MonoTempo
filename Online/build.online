#!/bin/sh

error() {
	exit 1
}

dir=$(realpath "$(dirname "$0")")

cd "$dir"/Receba || error
docker build --network=host --tag="receba:1.0" . || error

cd "$dir"/Reenvio || error
docker build --network=host --tag="reenvio:1.0" . || error
