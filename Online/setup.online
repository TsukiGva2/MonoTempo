#!/bin/sh

docker run --env-file=../shared.env -v ../database/:/var/monotempo-data --name receba "receba:1.0"

docker wait receba

docker run --env-file=../shared.env -v ../database/:/var/monotempo-data --name reenvio "reenvio:1.0"

docker wait reenvio

